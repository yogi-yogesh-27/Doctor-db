<% include partials/header %>
<style>
.container{
    text-align:center;
}

*{
    margin: 0;
    padding: 0;
}
.rate {
    border: 1px solid #cccccc;
    float: left;
    height: 46px;
    padding: 0 10px;
}
.rate:not(:checked) > input {
    position:absolute;
    top:-9999px;
}
.rate:not(:checked) > label {
    float:right;
    width:1em;
    overflow:hidden;
    white-space:nowrap;
    cursor:pointer;
    font-size:30px;
    color:#ccc;
}
.rate:not(:checked) > label:before {
    content: '★ ';
}
.rate > input:checked ~ label {
    color: #ffc700;    
}
.rate:not(:checked) > label:hover,
.rate:not(:checked) > label:hover ~ label {
    color: #deb217;  
}
.rate > input:checked + label:hover,
.rate > input:checked + label:hover ~ label,
.rate > input:checked ~ label:hover,
.rate > input:checked ~ label:hover ~ label,
.rate > label:hover ~ input:checked ~ label {
    color: #c59b08;
}
</style>
<br><br><br><br>

<div class="ui main text container segment" width="350px">
	<div class="ui huge header"></div>
	<form  action="/Doctors/details/<%= detail._id %>/comment" method="POST" class="ui form" style={width:0.5em;}>
	<div class="field">
    	Author:
		<input type="text" name="comment[author]"  value="<%= currentUser.username %>" >
  	</div>
  	<div class="field">
    	Comment:
		<input type="text" name="comment[text]"  value="<%= detail.email %>">
    </div>
    <div class="rate">
      <input type="radio" id="star5" name="comment[rate]" value="5" /><label for="star5" title="text">5 stars</label>
      <input type="radio" id="star4" name="comment[rate]" value="4" /><label for="star4" title="text">4 stars</label>
      <input type="radio" id="star3" name="comment[rate]" value="3" /><label for="star3" title="text">3 stars</label>
      <input type="radio" id="star2" name="comment[rate]" value="2" /><label for="star2" title="text">2 stars</label>
      <input type="radio" id="star1" name="comment[rate]" value="1" /><label for="star1" title="text">1 star</label>
  </div>
		<input class="ui violet basic button" type="submit">
	</div>
	</form>
	<a href="/home">Back to Home</a>
</div>



<% include partials/footer.ejs %>